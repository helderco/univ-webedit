{% extends '::user.html.twig' %}

{% block breadcrumb %}
<a class="current">List Pages</a>
{% endblock %}

{% block body %}
<article class="module width_full">
        <header><h3>List Pages</h3></header>
        <div class="module_content">
            <h3>Page List</h3>   
            <ul class="toggle">    
            {% for page in pages %}
                    <li>Page title: {{ page.title }} - <a href="{{ path('edit_page', { 'id' : page.id }) }}">Edit</a> - <a href="#">View</a> - <a href="{{ path('delete_page', { 'id' : page.id }) }}">Delete</a> </li>                
            {% endfor%}
            </ul>
    </div>
</article>    
{% endblock %}